{
  "name": "url-signer",
  "version": "1.0.0",
  "description": "Sign url to upload them to GCS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "BUCKET_NAME=kovids npx functions-framework --target=signUrl "
  },
  "author": "Junior BERNARD",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.2.1",
    "@google-cloud/storage": "^6.11.0"
  }
}
